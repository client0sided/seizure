# Tab

Tabs are used to organize content within a Window.  
Each Window can have multiple Tabs, which hold Buttons, Toggles, etc.

---

## Example

```lua
-- Create a new tab inside the main window
local settingsTab = window:AddTab("Settings")
